#include<stdio.h>
int main(void)
{
    char str[]={'G', 'o',  'o', 'd',' ', 't', 'i', 'm', 'e'};
    int arrLen = sizeof(str) / sizeof(char)
    int n;
    for(n =0; n<arrLen; i++)
    {
        printf("%c, str[i]");
    }
    printf("\n");
    return 0;
    
}