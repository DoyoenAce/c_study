#include<stdio.h>
int main(void)
{
    int pnum;
    for(int i=0;i=<10;i++)
    {
        if(pnum%pnum==1 && pnum%1==pnum)
        {
            printf("%d", pnum);
        }
    }
    return 0;
}