#include<stdio.h>
typedef struct dungchi
{
    int height[5];
    int weight[5];
} dungchi;
int main(void)
{
    dungchi* a;
    a = (char*)malloc(sizeof(dungchi));
    for(int i = 0; i < a; i++)
    {

    }
}